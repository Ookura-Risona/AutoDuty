name: Auto Sync Upstream with AI

on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

env:
  UPSTREAM_REPO: 'erdelf/AutoDuty'
  UPSTREAM_BRANCH: 'master'
  TARGET_BRANCH: '自动同步上游'  # 修改为目标分支名称
  AI_MODEL: 'deepseek-chat'

jobs:
  check-and-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}
          ref: ${{ env.TARGET_BRANCH }}  # 直接检出目标分支
      
      - name: Configure Git with PAT
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config url."https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/".insteadOf "https://github.com/"
      
      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/${{ env.UPSTREAM_REPO }}.git || true
          git fetch upstream
      
      - name: Check for new commits
        id: check_commits
        run: |
          UPSTREAM_COMMIT=$(git rev-parse upstream/${{ env.UPSTREAM_BRANCH }})
          CURRENT_COMMIT=$(git rev-parse HEAD)
          
          echo "upstream_commit=$UPSTREAM_COMMIT" >> $GITHUB_OUTPUT
          echo "current_commit=$CURRENT_COMMIT" >> $GITHUB_OUTPUT
          
          if [ "$UPSTREAM_COMMIT" = "$CURRENT_COMMIT" ]; then
            echo "has_updates=false" >> $GITHUB_OUTPUT
            echo "✅ Already up to date with upstream"
          else
            echo "has_updates=true" >> $GITHUB_OUTPUT
            
            git log --pretty=format:"%H|%s|%b" HEAD..upstream/${{ env.UPSTREAM_BRANCH }} > new_commits.txt
            git diff HEAD..upstream/${{ env.UPSTREAM_BRANCH }} --name-status > changed_files.txt
            
            echo "📝 New commits found"
            cat new_commits.txt
            echo ""
            echo "📁 Changed files:"
            cat changed_files.txt
          fi
      
      - name: Get changed file contents
        if: steps.check_commits.outputs.has_updates == 'true'
        run: |
          git diff HEAD..upstream/${{ env.UPSTREAM_BRANCH }} > full_diff.txt
          
          if [ $(wc -c < full_diff.txt) -gt 50000 ]; then
            echo "⚠️ Diff too large, creating summary"
            git diff HEAD..upstream/${{ env.UPSTREAM_BRANCH }} --stat > diff_summary.txt
          else
            cp full_diff.txt diff_summary.txt
          fi
      
      - name: Setup Python
        if: steps.check_commits.outputs.has_updates == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        if: steps.check_commits.outputs.has_updates == 'true'
        run: pip install openai
      
      - name: Analyze changes with DeepSeek AI
        if: steps.check_commits.outputs.has_updates == 'true'
        id: ai_analysis
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          python << 'PYTHON_SCRIPT'
          import os
          import sys
          import json
          from openai import OpenAI

          client = OpenAI(
              api_key=os.environ['DEEPSEEK_API_KEY'],
              base_url="https://api.deepseek.com"
          )

          with open('new_commits.txt', 'r', encoding='utf-8') as f:
              commits = f.read()
          with open('diff_summary.txt', 'r', encoding='utf-8') as f:
              diff = f.read()
          with open('changed_files.txt', 'r', encoding='utf-8') as f:
              files = f.read()

          prompt = f"""你是一个专业的代码分析助手。这是一个Final Fantasy XIV (FF14) Dalamud插件AutoDuty的汉化仓库。

          上游仓库有新的提交，请分析以下内容：

          ## 新提交信息：
          {commits}

          ## 变更文件列表：
          {files}

          ## 代码变更详情：
          {diff[:15000]}

          请分析并回答：
          1. 这些提交是否包含需要翻译的用户界面文本、消息或文档？
          2. 如果需要翻译，请列出所有需要翻译的文本内容，并提供准确的中文翻译。
          3. 是否存在可能的合并冲突？特别是与汉化相关的文件冲突。
          4. 提供合并建议。

          翻译注意事项：
          - 保持FF14官方游戏术语（如职能、副本、技能名称等）
          - UI文本简洁明了，符合中文习惯
          - 保留代码中的技术术语（如类名、函数名、变量名、API名称等）
          - 保持格式化字符串的占位符不变（如 {{0}}, %s, {{name}} 等）
          - 注释也需要翻译

          请以JSON格式回复（必须是有效的JSON）：
          {{
            "needs_translation": true或false,
            "translation_required": "简要说明为什么需要或不需要翻译",
            "translations": [
              {{"file": "文件路径", "original": "原文", "translated": "译文", "line": 行号或null, "context": "上下文说明"}}
            ],
            "potential_conflicts": ["可能冲突的文件列表"],
            "merge_strategy": "建议的合并策略（auto/manual/review）",
            "summary": "总体分析摘要，包括主要变更内容"
          }}
          """

          try:
              response = client.chat.completions.create(
                  model=os.environ.get('AI_MODEL', 'deepseek-chat'),
                  messages=[
                      {"role": "system", "content": "你是一个专业的本地化和代码分析专家，精通英文和中文，特别了解Final Fantasy XIV游戏和软件开发。请始终返回有效的JSON格式。"},
                      {"role": "user", "content": prompt}
                  ],
                  temperature=0.3,
                  max_tokens=4000
              )
              
              content = response.choices[0].message.content
              
              if "```json" in content:
                  content = content.split("```json")[1].split("```")[0].strip()
              elif "```" in content:
                  content = content.split("```")[1].split("```")[0].strip()
              
              data = json.loads(content)
              
              print("✅ DeepSeek AI Analysis Result:")
              print(json.dumps(data, ensure_ascii=False, indent=2))
              
              with open('ai_analysis.json', 'w', encoding='utf-8') as f:
                  json.dump(data, f, ensure_ascii=False, indent=2)
              
              with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                  f.write(f"needs_translation={str(data.get('needs_translation', False)).lower()}\n")
                  
          except Exception as e:
              print(f"❌ Error: {e}")
              sys.exit(1)
          PYTHON_SCRIPT
      
      - name: Merge and resolve conflicts with AI
        if: steps.check_commits.outputs.has_updates == 'true'
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          python << 'PYTHON_SCRIPT'
          import os
          import json
          import subprocess
          from openai import OpenAI

          client = OpenAI(
              api_key=os.environ['DEEPSEEK_API_KEY'],
              base_url="https://api.deepseek.com"
          )

          print("🔄 Attempting to merge upstream changes...")
          result = subprocess.run(
              ['git', 'merge', f"upstream/{os.environ['UPSTREAM_BRANCH']}"],
              capture_output=True,
              text=True
          )
          
          if result.returncode != 0:
              print("⚠️ Merge conflicts detected, asking DeepSeek AI for resolution...")
              
              conflicts_result = subprocess.run(
                  ['git', 'diff', '--name-only', '--diff-filter=U'],
                  capture_output=True,
                  text=True
              )
              conflicts = [f for f in conflicts_result.stdout.strip().split('\n') if f]
              
              if not conflicts:
                  print("❌ Merge failed but no conflicts found")
                  subprocess.run(['git', 'merge', '--abort'])
                  exit(1)
              
              print(f"Found {len(conflicts)} conflicted file(s)")
              
              for conflict_file in conflicts:
                  print(f"🔧 Resolving conflict in: {conflict_file}")
                  
                  try:
                      with open(conflict_file, 'r', encoding='utf-8') as f:
                          conflict_content = f.read()
                  except Exception as e:
                      print(f"❌ Failed to read {conflict_file}: {e}")
                      continue
                  
                  prompt = f"""这是一个汉化仓库的Git合并冲突。请智能解决这个冲突。

          文件: {conflict_file}

          冲突内容:
          {conflict_content}

          解决原则:
          1. 保留中文翻译（通常在 <<<<<<< HEAD 部分）
          2. 整合上游的代码结构变更（在 >>>>>>> upstream 部分）
          3. 如果上游修改了英文原文，请基于新的英文更新中文翻译
          4. 保持代码的功能完整性和语法正确性
          5. 删除所有冲突标记（<<<<<<<, =======, >>>>>>>）
          6. 保持原有的代码格式和缩进

          请直接提供解决后的完整文件内容，不要有任何解释或markdown标记。"""
                  
                  try:
                      response = client.chat.completions.create(
                          model=os.environ.get('AI_MODEL', 'deepseek-chat'),
                          messages=[
                              {"role": "system", "content": "你是一个专业的Git冲突解决专家和本地化专家。你的输出将直接写入文件，所以不要包含任何额外的说明或标记。"},
                              {"role": "user", "content": prompt}
                          ],
                          temperature=0.1,
                          max_tokens=8000
                      )
                      
                      resolved_content = response.choices[0].message.content.strip()
                      
                      if resolved_content.startswith("```"):
                          lines = resolved_content.split('\n')
                          if lines[0].startswith("```"):
                              lines = lines[1:]
                          if lines[-1].startswith("```"):
                              lines = lines[:-1]
                          resolved_content = '\n'.join(lines)
                      
                      with open(conflict_file, 'w', encoding='utf-8') as f:
                          f.write(resolved_content)
                      
                      subprocess.run(['git', 'add', conflict_file], check=True)
                      print(f"✅ Resolved: {conflict_file}")
                      
                  except Exception as e:
                      print(f"❌ Failed to resolve {conflict_file}: {e}")
                      subprocess.run(['git', 'merge', '--abort'])
                      exit(1)
              
              subprocess.run(['git', 'commit', '--no-edit'], check=True)
              print("✅ All conflicts resolved with AI assistance")
          else:
              print("✅ Merge successful without conflicts")
          PYTHON_SCRIPT
      
      - name: Push to target branch
        if: steps.check_commits.outputs.has_updates == 'true'
        run: |
          echo "🔄 Pushing changes to ${{ env.TARGET_BRANCH }} branch..."
          git push origin ${{ env.TARGET_BRANCH }}
          echo "✅ Successfully pushed to ${{ env.TARGET_BRANCH }}"
      
      - name: Create summary
        if: always()
        run: |
          echo "## 🔄 上游同步检查 (DeepSeek AI)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.check_commits.outputs.has_updates }}" = "true" ]; then
            echo "✅ 发现新的上游提交，已自动合并到 **${{ env.TARGET_BRANCH }}** 分支" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- 需要翻译: **${{ steps.ai_analysis.outputs.needs_translation }}**" >> $GITHUB_STEP_SUMMARY
            echo "- AI模型: \`${{ env.AI_MODEL }}\`" >> $GITHUB_STEP_SUMMARY
            
            if [ -f "ai_analysis.json" ]; then
              TRANSLATION_COUNT=$(cat ai_analysis.json | python3 -c "import sys, json; data=json.load(sys.stdin); print(len(data.get('translations', [])))" 2>/dev/null || echo "0")
              echo "- 检测到翻译项: **${TRANSLATION_COUNT}** 个" >> $GITHUB_STEP_SUMMARY
              
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "### 📊 DeepSeek AI分析结果" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
              cat ai_analysis.json >> $GITHUB_STEP_SUMMARY
              echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "✅ 已是最新，无需同步" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
